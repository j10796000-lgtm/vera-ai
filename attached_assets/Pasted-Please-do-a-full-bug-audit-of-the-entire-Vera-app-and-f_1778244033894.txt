Please do a full bug audit of the entire Vera app and fix every issue 
you find. Go through each of these areas one by one:

PART 1 — CHAT
- Make sure the textarea input works and updates state on every keystroke
- Make sure Enter sends the message and Shift+Enter creates a new line
- Make sure the send button works and has type="button"
- Make sure there are NO <form> tags wrapping the input — replace with <div>
- Make sure loading starts as false and resets to false after every API call
- Make sure the API call has a 30 second timeout and a proper try/catch/finally
- Make sure empty assistant bubbles never appear
- Make sure the first message loads correctly on every visit
- Make sure Vera's opening greeting always shows on first load

PART 2 — MEMORY & PERSISTENCE
- Make sure chat history is saved to localStorage after every message
- Make sure chat history loads correctly when the user comes back
- Make sure mood tracker entries are saved to localStorage permanently
- Make sure neither chat history nor mood entries ever disappear on refresh
- Make sure past chats are archived correctly when starting a new chat
- Search the entire codebase for localStorage.clear() and remove it 
  unless it is inside a button the user explicitly clicks

PART 3 — API
- Make sure the Anthropic API key is loaded from environment variables
- Make sure the model is set to claude-opus-4-6
- Make sure max_tokens is set to 1000
- Make sure the system prompt is always included in every API call
- Make sure the full message history is sent with each request
- Make sure the response is validated before being added to the chat
- Make sure any API error shows a human friendly message to the user
  instead of crashing or freezing

PART 4 — UI & NAVIGATION
- Make sure all tabs and navigation links work correctly
- Make sure the mood tracker tab loads the MoodTracker component
- Make sure the chat tab loads the chat component
- Make sure the past chats panel opens and closes correctly
- Make sure all buttons have visible feedback when clicked
- Make sure the app does not crash when switching between tabs
- Make sure the app looks correct on both desktop and mobile screens

PART 5 — PERFORMANCE
- Make sure only the last 20 messages are sent to the API to avoid 
  slow responses and timeouts
- Make sure the loading indicator shows while Vera is thinking
- Make sure the loading indicator disappears after every response 
  including errors
- Make sure the page scrolls to the latest message automatically

PART 6 — GENERAL CODE HEALTH
- Check for any JavaScript or React errors in the entire codebase
- Check for any missing imports or undefined variables
- Check for any infinite re-render loops in useEffect hooks
- Make sure all useEffect hooks have correct dependency arrays
- Remove any unused variables, imports, or dead code
- Make sure there are no duplicate function definitions

After completing all checks, give me a summary of:
1. Every bug you found
2. Every fix you made
3. Anything you were unsure about so I can review it
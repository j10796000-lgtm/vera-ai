There are two bugs to fix in Vera's chat:

BUG 1 — Empty response bubbles appear when Vera freezes
BUG 2 — Vera sometimes stops responding entirely mid-conversation

Please fix both with the following:

1. FIND the sendMessage function (or wherever the API call to Anthropic is made)

2. ADD a timeout so if Vera takes longer than 30 seconds, it cancels and shows a message:

const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 30000);

const response = await fetch("https://api.anthropic.com/v1/messages", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  signal: controller.signal,
  body: JSON.stringify({
    model: "claude-opus-4-6",
    max_tokens: 1000,
    system: SYSTEM_PROMPT,
    messages: newMessages.map((m) => ({
      role: m.role,
      content: m.content,
    })),
  }),
});

clearTimeout(timeoutId);

3. WRAP the entire API call in a try/catch/finally block that looks exactly like this:

try {
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), 30000);

  const response = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    signal: controller.signal,
    body: JSON.stringify({
      model: "claude-opus-4-6",
      max_tokens: 1000,
      system: SYSTEM_PROMPT,
      messages: newMessages.map((m) => ({
        role: m.role,
        content: m.content,
      })),
    }),
  });

  clearTimeout(timeoutId);

  if (!response.ok) {
    throw new Error(`API error: ${response.status}`);
  }

  const data = await response.json();
  const reply = data.content?.[0]?.text;

  if (!reply || reply.trim() === "") {
    throw new Error("Empty response");
  }

  setMessages((prev) => [...prev, { role: "assistant", content: reply }]);

} catch (err) {
  let message = "Something got in the way. I'm still here — try again.";
  if (err.name === "AbortError") {
    message = "That took too long on my end. I'm still here — try again.";
  }
  setMessages((prev) => [
    ...prev,
    { role: "assistant", content: message },
  ]);
} finally {
  setLoading(false);
}

4. MAKE SURE setLoading(false) only lives inside the finally block and nowhere else — this prevents the loading state from getting stuck.

5. FIND where assistant messages are added to state before the API call and REMOVE any placeholder empty assistant message that gets added before the response comes back. Empty bubbles appear because an empty message is being pushed to state before the API responds.

6. ADD a retry button that appears when Vera's error message is shown, so the user can tap to resend without retyping:

If the last message in the messages array has role "assistant" and contains the word "try again", show a small "retry" button below it that calls sendMessage() with the last user message.